import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        flex: 1, justifyContent: 'center', backgroundColor: 'white', alignItems: 'center',
    },
    languagecontainer: {
        position: 'absolute', top: 0, right: 0, marginTop: 20, marginRight: 5
    }
})
export default styles